version: "3.8"
services:
  weapi:
    build: .
    container_name: weapi
    ports:
      - "8081:3000"
    restart: always
    # Sin volumen session: la sesión vive solo dentro del contenedor.
    # Para borrar todo: recrea el contenedor (Stop + Start o Redeploy).
    # Si quieres persistir sesión, descomenta:
    # volumes:
    #   - ./session:/usr/src/app/session

